There is fresh vite project installed in directory `vite-project`

and we have three react wrapper components:

- SelectedSection from `composition/selected-section/react.ts`
- OptionsSection from `composition/options-section/react.ts`
- CompositeSelect from `composition/composite-select/react.ts`

I would like to create homepage with links to different test pages as a main page in `vite-project/`

Then from there create page which replicates all functionalities from composition/selected-section/SelectedSectionManagerWebComponent.nocssrequest.html

# Imprtant

Assume code is available in node_modules/composite-select

it is packed to tar.gz file and installed in node_modules

manually to represent final build and in our react attempts we will try to use it from there

if anything doesnt work you are allowed to modify any of

- SelectedSection from `composition/selected-section/react.ts`
- OptionsSection from `composition/options-section/react.ts`
- CompositeSelect from `composition/composite-select/react.ts`
  also
  package.json

if you change anything in these files ask me to rebuild and pack package in node_modules/composite-select again.

# see files

you can use command

```

find node_modules/composite-select

```

to inspect what files are where
